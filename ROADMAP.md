# 🗺 نقشه راه: رسیدن به سطح baboliha.ir و عبور از آن

> مبنا: کالبدشکافی فنی baboliha.ir (پروژه «Babol Bargh» آژانس وردی) — تیر ۱۴۰۵ / اوت ۲۰۲۶
> اصل راهبرد: **پاریته UX در ۴ فاز اول، عبور در فازهای ۵ و ۶.** چیزی که اضافه می‌کنیم
> نباید نقشه، اسلایدر زمان و ادیتور مرزها — یعنی مزیت‌های اصلی ما — را خراب کند.

## وضعیت امروز (پاریته)

| قابلیت | baboliha | ما (الان) | فاز حل |
|---|---|---|---|
| مستقل از ساعت دستگاه | ✅ (آفست ساعت سرور) | ✅ (فاز ۰) | ۰ |
| کارت وضعیت + شمارش معکوس + نوار پیشرفت | ✅ | ✅ (فاز ۱) | ۱ |
| ذخیره چند منطقه («مناطق من») | ✅ (پین + سینک سرور) | ✅ (فاز ۱ — localStorage) | ۱ |
| جستجوی فارسی هوشمند (نرمال‌سازی، پیشنهاد، پرش) | ✅ | ✅ (فاز ۱) | ۱ |
| GPS «موقعیت من» | ✅ (مچ **متنی** خیابان) | ✅ (فاز ۱ — نقطه-در-پلی‌گون، دقیق‌تر) | ۱ ✨ |
| نمای لیستی کنار نقشه | ✅ (فقط لیست) | ✅ (فاز ۱ — نقشه + لیست) | ۱ |
| هماندازه بودن با صفحه گوشی (viewport، safe-area، کیبورد، زوم) | ✅ | ✅ (فاز ۱.۵) | ۱.۵ |
| PWA + نصب + راهنمای iOS | ✅ | ✅ (فاز ۲) | ۲ |
| کار در حالت آفلاین داده | ❌ | ✅ (فاز ۲ — SWR روی snapshot) | ۲ ✨ |
| نوتیفیکیشن تغییر وضعیت | ❌ | ❌ | ۳ ✨ (عبور) |
| کانال تلگرام خودکار | فقط کانال مرده (۵ عضو) | ❌ | ۳ ✨ (عبور) |
| SEO (صفحه شهر، FAQ، عنوان داینامیک) | ✅ (قوی) | ◐ (۴.۱–۴.۲؛ FAQ/۴.۵ مانده) | ۴ |
| اشتراک‌گذاری/دیپ‌لینک هر منطقه | ❌ | ✅ (۴.۳–۴.۴) | ۴ ✨ |
| نقشه فیدرها + اسلایدر زمان + پخش | ❌ | ✅ | — (مزیت اصلی) |
| داده‌باز (API عمومی) | ❌ | نیمه (snapshot.json هست، مستند نیست) | ۵ |
| دسترسی پرسرعت از ایران | ✅ (هاست تهران) | ⚠️ (github.io گاهی کُند) | ۵ |

---

## فاز ۰ — پایه‌ی صحت: مرگ وابستگی به ساعت دستگاه 🧭 ✅ (انجام شد)

**چرا اول:** همه شمارش‌معکوس‌ها و تغییر وضعیت‌های فاز بعدی روی ساعت درست سوارند.
اگر ساعت گوشی کاربر غلط باشد همه‌چیز بعدی دروغ می‌شود. این فاز کوچک‌ترین و مهم‌ترین است.

- ✅ **۰.۱** آفست ساعت سرور: از هدر HTTP `Date` پاسخِ `snapshot.json` (دقیق‌ترین منبع،
  همیشه تازه) و در نبود آن از `ts` اسنپ‌شات → `serverOffsetMs`؛ همه‌ی محاسبات دقیقه‌ی روز
  از `tehranMinOfDay(nowMs())` خوانده می‌شود و منطقه‌ی زمانی با `Intl` روی `Asia/Tehran` قفل است
  (یعنی حتی timezone غلطِ دستگاه هم بی‌اثر شد). بونوس: `server.py` حالا `/snapshot.json` را
  هم سرو می‌کند تا نسخه‌ی محلی همان مسیر داده را داشته باشد.
- ✅ **۰.۲** نشان «🛰 به وقت سرور» زیر ساعت تایم‌لاین + tooltip «مستقل از ساعت دستگاه شما»
  (و مهر «آخرین داده» حالا همیشه به وقت ایران فرمت می‌شود).
- ✅ **۰.۳** هشدار یک‌باره (toast) اگر اختلاف ساعت دستگاه بیش از ۹۰ دقیقه باشد.

**فایل‌ها:** فقط `web/index.html` (هیچ تغییری در فرمت snapshot لازم نیست — `ts` از قبل هست).
**معیار قبولی:** با دستکاری ساعت سیستم به ‎±۳ ساعت، وضعیت‌ها/اسلایدر دقیقاً مثل قبلِ دستکاری درست بماند.
**اندازه:** S (نیم‌روز).

---

## فاز ۱ — پاریته تجربه‌ی کاربری 🎨 ✅ (انجام شد — تأیید با smoke-test مرورگر jsdom ۲۳/۲۳)

> خلاصه‌ی پیاده‌سازی: کارت وضعیت با شمارش معکوس/نوار پیشرفت در پنل (نسبت به زمان اسلایدر،
> تیک ۳۰ثانیه‌ای)، ⭐ مناطق من با localStorage + نوار چیپ (دکمه «خانه من» = اولین ذخیره‌شده)،
> جستجوی نرمال‌شده‌ی فارسی (ي/ك/آ/اعراب/نیم‌فاصله/ارقام فارسی-عربی؛ «ترک‌محله»=«ترک محله»؛
> AND چندکلمه‌ای؛ ۸ پیشنهاد هایلایت‌شده؛ کیبورد ↑↓/Enter/Esc)، GPS با نقطه-در-پلی‌گون
> (دقیق‌تر از مچ متنی baboliha) + نزدیک‌ترین فیدر با فاصله در خارج از شهر، و نمای لیستی
> گرید با مرتب‌سازی شدت + شمارش معکوس + بازگشت به نقشه با کلیک. همه فقط در `web/index.html`.

موتور وضعیت ما (`status_model` + `colorArr`) از قبل قوی‌تر از ایده‌ی «سه‌وضعیت» آن‌هاست؛
فقط **نمایش** را به سطح آن‌ها می‌رسانیم و یکی جلوتر می‌زنیم.

- ✅ **۱.۱ کارت وضعیت زنده در پنل جزئیات** (بازنویسی محتوای `#panel`):
  - نشان وضعیت با همان زبان مردم: `🔴 برق رفت` / `🟡 آماده باش` / `🟢 وصل شد` (+ نگه‌داشتن
    اصطلاحات دقیق فعلی «قطع گزارش‌شده/برنامه‌ریزی‌شده» به‌عنوان زیرنویس)
  - شمارش معکوس فارسی: «۱ ساعت و ۱۸ دقیقه تا وصل شدن» / «۲ ساعت و ۵ دقیقه تا شروع»
    (تابع `fmtCountdown(min)` با ارقام فارسی، از `fmtMin` موجود مشتق می‌شود)
  - نوار پیشرفت `elapsed/total` برای پنجره‌ی جاری + تیک `شروع` و `پایان تقریبی`
  - تیک‌زنِ ۳۰ثانیه‌ای که فقط پنلِ باز را به‌روز کند (نه کل نقشه)
- ✅ **۱.۲ ⭐ مناطق من (چند‌مكانه):**
  - کلیک ⭐ در پنل → آرایه‌ی `saved` در `localStorage` (تعمیم `MY_FEEDER` فعلی)
  - نوار «مناطق من» بالای نقشه: چیپ هر فیدر با نقطه‌ی رنگ وضعیت لحظه‌ای؛ کلیک = پرواز به پلی‌گون + باز شدن پنل
  - اگر همه‌ی پنجره‌های فیدر تمام شده → چیپ سبز «وصل شد» (همان منطق هوشمند آن‌ها)
  - دکمه‌ی «خانه من» فعلی = میان‌بری روی اولین عضو `saved` (سازگاری رو به عقب)
- ✅ **۱.۳ جستجوی هوشمند** (باکس 🔍 کنار چیپ‌های آمار):
  - نرمال‌سازی فارسی: `ي→ی، ك→ک، آ→ا، إأ→ا، حذف اعراب` + تبدیل زنده‌ی ارقام فارسی/عربی به لاتین
  - چندکلمه‌ای AND روی `name + areas[] + feeder` همه‌ی زون‌ها (داده از قبل در snapshot هست)
  - دراپ‌داون حداکثر ۸ پیشنهاد با هایلایت + ناوبری باجه‌های ↑↓/Enter/Escape
  - انتخاب = پرواز به پلی‌گون و باز شدن پنل (بهتر از «پرش به کارت متنی» آن‌ها)
- ✅ **۱.۴ 📍 موقعیت من (GPS):**
  - `navigator.geolocation.getCurrentPosition` → تست نقطه-در-پلی‌گون (ری‌کتینگ ~۲۰ خط) روی ۴۷ زون
  - باز شدن پنل همان فیدر + هایلایت؛ خارج از شهر → نزدیک‌ترین زون با ذکر فاصله
  - این همان قابلیتِ آن‌هاست ولی **دقت نقشه‌ای** به‌جای مچ متنی خیابان — در UI صادقانه بنویسیم «مرزها تقریبی»
- ✅ **۱.۵ نمای لیستی (🗺/📋 سوییچ):**
  - نمای لیست = همان کارت‌های ۱.۱ برای همه‌ی فیدرها، مرتب‌شده: قرمز → نارنجی → خاکستری/سبز
  - تب «امروز/فردا» (داده‌ی فردا از قبل در `history.tomorrow` هست؛ آن‌ها همین را نشان می‌دهند)
  - دسکتاپ: لیست کنار نقشه (split) / موبایل: تب‌جایگزین — پیاده‌سازی با CSS grid موجود

**فایل‌ها:** `web/index.html` (اصلی)؛ بدون تغییر بک‌اند.
> 🔄 **به‌روزرسانی پس از درخواست مالک:** چیدمان برعکس شد — صفحه‌ی اول سایت حالا تب «🏠 خانه» =
> فهرست کارت‌ها (بدون نقشه، مشابه جدول‌های رایج) و نقشه در تب دوم است (`#viewtabs`، دیپ‌لینک `#map`)؛
> کلیک روی کارت/نتیجه‌ی جستجو مستقیم به همان فیدر روی نقشه می‌پرد.

**معیار قبولی:** کاربر بتواند «محله‌اش را ستاره‌دار کند، گوشی را قفل کند، برگردد و در کمتر از ۲ ثانیه
وضعیت + شمارش معکوس منطقه‌اش را ببیند» — بدون هیچ رفرشی.
**اندازه:** M (۲–۳ روز کار متمرکز).

---

## فاز ۱.۵ — موبایل: دقیقاً هم‌اندازه‌ی صفحه‌ی گوشی 📱 ✅ (انجام شد)

> درخواست مالک (شهریور ۱۴۰۵): «سایت رقیب وقتی در گوشی باز می‌شود دقیقاً به اندازه‌ی صفحه‌ی گوشی
> است» — یعنی بدون زوم‌شدگی، بدون اسکرول افقی، بدون بریدگی پایین صفحه، و هم‌راستا با ناچ و
> دکمه‌ی هوم آیفون.

- ✅ **۱.۵.۱ هم‌اندازه‌ی واقعی صفحه (dynamic viewport):** `height:100dvh` با fallback به `100%`
  تا وقتی نوار آدرس مرورگر گوشی جمع/باز می‌شود، نقشه و تایم‌لاین همیشه در محدوده‌ی صفحه‌ی
  قابل‌دیدن بمانند (قبلاً در iOS پایینِ تایم‌لاین پشت نوار آدرس گم می‌شد — با `overflow:hidden`
  دست‌نیافتنی می‌ماند).
- ✅ **۱.۵.۲ safe-area (ناچ و دکمه‌ی هوم آیفون):** `viewport-fit=cover` + `env(safe-area-inset-*)`
  روی هدر (بالا) و تایم‌لاین/دکمه‌های نقشه/پنل/پاپ‌آپ نصب/toast (پایین) — در حالت PWA
  نصب‌شده دیگر هیچ چیزی زیر نوار وضعیت یا دکمه‌ی هوم نمی‌رود.
- ✅ **۱.۵.۳ کیبورد گوشی:** `interactive-widget=resizes-content` تا با باز شدن کیبورد جستجو،
  چیدمان جمع شود نه اینکه رویش بپوشاند.
- ✅ **۱.۵.۴ زوم خودکار iOS روی input:** فونت باکس جستجو در موبایل ۱۶px (iOS وقتی فونت input
  کوچک‌تر از ۱۶px باشد هنگام فوکوس زوم می‌کند و «اندازه‌ی دقیق صفحه» به‌هم می‌ریزد).
- ✅ **۱.۵.۵ اهداف لمسی:** دکمه‌ی پخش ۴۴px، تب‌ها، چیپ‌های مناطق من، پیشنهادهای جستجو و
  دکمه‌های نقشه حداقل ۴۴px ارتفاع.
- ✅ **۱.۵.۶ ضد‌سرریز:** `text-size-adjust` + جمع‌وجورکردن هدر (عنوان با ellipsis) و چیپ‌ها در
  صفحه‌های ≤400px.

**فایل‌ها:** فقط `web/index.html` (+ `ROADMAP.md`).
**معیار قبولی:** در گوشی (اندروید/iOS)، صفحه‌ی خانه و نقشه دقیقاً هم‌اندازه‌ی viewport است؛
اسکرول افقی وجود ندارد؛ در حالت نصب‌شده (PWA) هدر زیر نوار وضعیت نمی‌رود و تایم‌لاین بالای
دکمه‌ی هوم است؛ با فوکوس روی جستجو زوم نمی‌شود.
**اندازه:** S (نصف‌روز).

---

## فاز ۲ — PWA و آفلاین 📱 ✅ (انجام شد — ۱۵/۱۵ تست SW با شبیه‌ساز caches/fetch)

> پیاده‌سازی: `web/manifest.json` (RTL/fa، آیکون‌های ⚡ تولیدشده با Pillow در ۸ اندازه + ۲ maskable)،
> `web/sw.js` (ناوبری: network-first + fallback کش؛ `snapshot.json`: **stale-while-revalidate** با
> نشان `X-From-Cache` → UI حالت «آفلاین — آخرین داده» را نشان می‌دهد؛ استاتیک خودم + jsdelivr:
> cache-first)، پاپ‌آپ نصب فقط-موبایل با تأخیر ۳s + کوول‌داون ۲۴s + «دیگر نشان نده» + راهنمای
> iOS-سافاری (حالت تست: `?test_pwa=1`)، دکمه «🔄 داده جدید» فقط در iOS-PWA (پاک‌سازی کش + SW)،
> reload در bfcache (pageshow persisted).

آن‌ها PWA دارند ولی **آفلاینِ داده ندارند** (SWشان عمداً HTML/AJAX را کش نمی‌کند).
ما سایت استاتیک داریم → آفلاین کاملِ «آخرین snapshot» برای ما ممکن است و آن‌ها هرگز نمی‌توانند.

- ✅ **۲.۱** `web/manifest.json` — نام «نقشه قطعی برق قائم‌شهر»، تم `#0b1526`، آیکون‌ها از همان ⚡ فعلی
  (SVG فعلی را به ۷۲/۹۶/۱۲۸/۱۴۴/۱۹۲/۳۸۴/۵۱۲ پنج‌ضلعی رندر می‌کنیم — یک‌بار با اسکریپت Python)
- ✅ **۲.۲** `web/sw.js`:
  - `install`: کش CSS/JS/فونت/تایل‌های پایه‌ی zoom اولیه
  - `fetch`: استاتیک = cache-first؛ `snapshot.json` = **stale-while-revalidate** (کلید طلایی)
  - `navigate`: network-first + fallback به `index.html` کش‌شده = نقشه‌ی آفلاین با آخرین داده
- ✅ **۲.۳** پاپ‌آپ نصب: فقط موبایل، تأخیر ۳s، کوول‌داون ۲۴h (کلید `a2hsDismissedTime`)،
  چک‌باکس «دیگر نشان نده»، مسیر iOS-سافاری جدا (راهنمای Share→Add to Home Screen)
  — الگوی اثبات‌شده‌ی آن‌ها، بدون اختراع دوباره
- ✅ **۲.۴** دکمه‌ی «دریافت لیست جدید» شناور فقط در iOS-standalone (پاک‌سازی کش + reload با `?v=`)
- ✅ **۲.۵** ثبت SW + `beforeinstallprompt` در `index.html`

**فایل‌های جدید:** `web/manifest.json`، `web/sw.js`، `web/icons/*` | **تغییر:** `web/index.html`.
**معیار قبولی:** حالت پرواز گوشی → سایت باز می‌شود، نقشه با آخرین snapshot رنگ می‌شود و
«به‌روزرسانی: HH:MM (آفلاین)» نشان می‌دهد؛ وای‌فای وصل شود → بی‌صدا تازه می‌شود.
**اندازه:** M.

---

## فاز ۳ — هشدار: چیزی که هیچ‌کس در مازندران ندارد 🔔

- **۳.۱ نوتیفیکیشن مرورگر (بدون سرور):**
  - تیک ۳۰ثانیه‌ای فاز ۱ وضعیت فیدرهای `saved` را چک می‌کند؛
    تغییر سبز/نارنجی → 🔴 = `new Notification(...)` (پس از درخواست مجوز با دکمه‌ای صریح در «مناطق من»)
  - پیام: «🔴 فیدر ۴۰۲ (ترک‌محله) — گزارش قطعی؛ پایان تقریبی ۱۴:۰۰»
- **۳.۲ کانال تلگرام قائم‌شهر (پست خودکار):**
  - در `build_online.py` (کرونِ از قبل موجود): اگر snapshot نوشته شد و فیدرهای «مشترکان»
    تغییر وضعیت قرمز دادند → `requests.post` به Bot API
  - فایل کانفیگ `notify.ini` (token + chat_id + لیست فیدرهای دنبال‌شده) — در `.gitignore`
  - پیام فشرده + لینک نقشه با دیپ‌لینک فیدر (فاز ۴.۳)
  - **چرا بُرد:** کانال baboliha پنج عضو دارد؛ فضای تلگرام قائمشهر خالی است
- **۳.۳ (اختیاری، بعداً) Web Push واقعی با VAPID** — فقط اگر ۳.۱/۳.۲ کم بود؛ به سرور جدا نیاز دارد.

**فایل‌ها:** `web/index.html`، `build_online.py`، جدید: `notify.py`، `notify.ini.example`.
**معیار قبولی:** در یک قطعی واقعی، عضو کانال قبل از پایان پنجره پیام بگیرد؛ کاربر سایت با تب باز نوتیف بگیرد.
**اندازه:** M.

---

## فاز ۴ — دیده‌شدن: SEO و اشتراک‌گذاری 📈 (۴.۱ تا ۴.۴ انجام شد — ۴.۵/۴.۶ مانده)

- ✅ **۴.۱** متای کامل: `description`، `og:*`، `theme-color`، favicon رسمی
- ✅ **۴.۲** عنوان داینامیک با تاریخ شمسی امروز (همان ترفند آن‌ها):
  `نقشه قطعی برق قائم‌شهر — امروز پنجشنبه ۱۴۰۵/۰۵/۲۹` (از `weekdays` + `curDate` سمت JS)
- ✅ **۴.۳ دیپ‌لینک فیدر: `#f=402` و `#map#f=402`** → در لود، نمای نقشه + پنل همان فیدر باز شود؛
  آدرس با کلیک روی تب‌ها هم `#f=` را نگه می‌دارد
- ✅ **۴.۴ دکمه اشتراک‌گذاری (Web Share API + کپی لینک)** در پنل هر فیدر — آن‌ها ندارند
- **۴.۵** متن معرفی + FAQ کوتاه پایین صفحه (تک‌صفحه‌ای، همان صفحه‌ی اصلی — سئوی محلی قائمشهر)،
  + `application/ld+json` از نوع FAQPage — **مانده**
- **۴.۶** `robots.txt` + `sitemap.xml` استاتیک (فقط یک URL اصلی؛ بدون صفحه‌بندی ساختگی) — **مانده**

**فایل‌ها:** `web/index.html` + دو فایل استاتیک کوچک.
**معیار قبولی:** لینک `.../#f=402` در تلگرام باز شود و مستقیم پنل فیدر ۴۰۲ با شمارش معکوس باشد؛
جستجوی «قطعی برق قائمشهر» صفحه‌ی ما را با توضیح درست نشان دهد.
**اندازه:** S–M.

---

## فاز ۵ — زیرساخت رشد 🏗

- **۵.۱ دامنه‌ی ایرانی میرور:** خرید دامنه `.ir` ارزان (یا زیردامنه از یک سرویس ایرانی) →
  reverse-proxy به GitHub Pages یا اسکریپت همگام‌سازی با هر push. مشکل «کندی github.io در ایران» حل.
- **۵.۲ مستندسازی snapshot.json به‌عنوان API عمومی** (`docs/API.md`): داده‌باز = اعتماد + بک‌لینک‌های طبیعی؛
  چیزی که baboliha (بسته و PHP) نمی‌تواند تقلید کند
- **۵.۳ مانیتورینگ کرون:** اگر دو پیکرپی‌الان درست پشت‌هم snapshot نشد → پیام تلگرام به ادمین
  (همان مسیر ۳.۲)
- **۵.۴** صفحه‌ی «درباره/شفافیت»: منبع داده رسمی، فرض‌های مدل (+۲ ساعت، مرزهای تقریبی)، لایسنس

**اندازه:** M (۵.۱ به خرید دامنه وابسته است؛ بقیه مستقل).

---

## فاز ۶ — عبور: چیزهایی که تازه ما خواهیم داشت 🚀

- **۶.۱ پروفایل تاریخی فیدر:** `data/history.jsonl` از قبل هر poll را ذخیره می‌کند →
  «فیدر ۴۰۲ این ماه ۹ بار، مجموعاً ۲۱ ساعت قطع بوده» (کارتی در پنل + جدول نمای لیستی)
- **۶.۲ هیت‌مپ روزانه روی نقشه:** حالت «شدت قطعی امروز/این هفته» به‌جای وضعیت لحظه‌ای
  (رنگ = دقایق قطع؛ از همان `colorArr` مشتق می‌شود)
- **۶.۳ پیش‌بینی ساده:** میانگین مدت گزارش‌های زنده‌ی فیدر → «معمولاً ~۹۰ دقیقه وصل می‌شود»
- **۶.۴ گزارش مردمی (اختیاری):** دکمه «برق من رفت/وصل شد» → ثبت در `localStorage` +
  endpoint ساده (فقط نسخه‌ی محلی/سروری؛ نسخه‌ی Pages آن را در صف می‌گذارد)

**اندازه:** هرکدام S جداگانه، بدون وابستگی به هم.

---

## ترتیب و قواعد اجرا

1. **۰ → ۱ → ۱.۵ → ۲ → ۳ → ۴ به ترتیب سخت‌گیرانه** (هر فاز بر قبلی سوار است؛ ۲ و ۴ کمی موازی‌شدنی‌اند).
2. بعد از هر فاز: اجرای `python status_model.py --selftest` + تست دستی ساعت/آفلاین/موبایل
   + اطمینان از سالم‌ماندن **اسلایدر زمان، پخش خودکار و ادیتور مرز** (نسخه‌ی محلی).
3. هیچ‌وقت دو فاز را همزمان در یک شاخه‌ی کاری جلو نبرید؛ هر فاز = یک PR کوچک با چک‌لیست معیار قبولی‌اش.
4. برآورد کل تا پاریت کامل (پایان فاز ۴): **~۲ هفته کار متمرکز** برای آشنا به کدفعلی؛
   فازهای ۵ و ۶ ذخیره‌ی رشدند، نه پیش‌نیاز رقابت.

## اصل طلایی

> آن‌ها «لیست بهتر» ساختند؛ ما «نقشه‌ی زنده‌ی واقعی» داریم.
> از آن‌ها فقط **لایه‌ی انسانی** تجربه را برمی‌داریم (شمارش معکوس، مناطق من، نصب، جستجو)
> و هیچ‌وقت جنسِ متفاوتِ محصول خودمان — جغرافیا و زمان — را قربانی لیست‌شدن نمی‌کنیم.
